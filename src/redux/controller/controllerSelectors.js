export const modalIsOpen = state => state.controllers.modalOpen;

export const getPage = state => state.controllers.currentPage;

export const getTotalPage = state => state.controllers.totalPage;

export const burgerMenuIsOpen = state => state.controllers.burgerMenuOpen;